## Возможные следующие шаги (не запрошено, просто заметки)
- Ввод голосом
Отлично, ты сделал две правки и получилось две моделки для конфигурации диктовки и воспроизведения. Но я нашел проблему, что в них разные кнопки сохранения. Одна называется Done, а другая называется Use Model. Наверное, их стоит назвать одинаково, возможно, как-то по-другому. Да, ну вроде не очень понятно, что дан. Use Model понятно, но, в общем, может быть, стоит их переименовать. Это первое. А второе, когда я нахожусь в файле, и в нем текст не выбран, и я нажимаю «Воспроизведение, то ничего не воспроизводится. А вот если текст выбрать, тогда воспроизводится. Это обычный текстовый файл. Курсор в этом случае в файле может быть, например, в конце. Посмотри, как это можно исправить.
- перевод текста
- проанализировать как сейчас выполняется работа с git
- Git blame по строке.
- Project-wide TS/JS автокомплит (`addExtraLib`, чтение `node_modules/@types`).
- Мультикурсор — Monaco поддерживает из коробки, стоит перепроверить, что шорткаты не
  блокируются.
- **ESLint fix-on-save** — раз диагностика ESLint уже подключена (используя локальный
  eslint открытого проекта), логично туда же добавить опциональный `--fix` при сохранении.
- **Git stash/pop** — сейчас в git-панели только stage/unstage/discard/commit/push/pull;
  быстрый stash перед переключением контекста — частый сценарий, которого не хватает.
- **Commit history / log** — простой список последних коммитов текущей ветки (`git log`)
  в git-панели или отдельной вкладке, без клейма на полноценный git log viewer.
- **Amend last commit** — чекбокс рядом с полем commit message в git-панели.

- в меню мак сейчас отображается electron, наверно тоже стоит над этим поработать и настроить меню

- шрифт не нравится, особенно как выглядит кириллица, у fleet лучше
- размер текста в сидебаре и редакторе как-то не сочетаются, во fleet лучше сочетание

- в гит, список файлов как в idea
